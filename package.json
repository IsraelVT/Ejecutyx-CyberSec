{
  "name": "ejecutyx-cyber-cloud",
  "private": true,
  "workspaces": ["apps/web", "services/api"],
  "scripts": {
    "dev:web": "npm --workspace @ejecutyx/web run dev",
    "dev:api": "npm --workspace @ejecutyx/api run dev",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "engines": { "node": ">=22" }
}
